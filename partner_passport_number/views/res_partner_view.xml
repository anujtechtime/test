<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- <record id="view_partner_form_passport" model="ir.ui.view">
        <field name="name">res.partner.form.passport</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='vat']" position="after">
                <field name="passport_number"/>
                <field name="attempts"/>
                <field name="registration_date"/>
            </xpath>

            <xpath expr="//sheet" position="inside">
                <group string="معلومات المرحلة الاعدادية" col="2">
                    <field name="institute"/>
                    <field name="specialization" attrs="{'invisible': [('institute', '=', False)]}"/>
                    <field name="certificate" filename="certificate_filename"
                           attrs="{'invisible': [('institute', '=', False)]}"/>
                </group>
            </xpath>

        </field>
    </record> -->
</odoo>
